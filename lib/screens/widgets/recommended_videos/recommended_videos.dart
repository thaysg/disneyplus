import 'package:flutter/material.dart';

class RecomendedVideos extends StatelessWidget {
  const RecomendedVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
